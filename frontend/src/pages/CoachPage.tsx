import { useState } from "react";
const answers: Record<string,string> = {
  better:"Your improvement came from fewer missed intentions, especially when you started with one focused action. Consistency before intensity.",
  tomorrow:"Keep tomorrow simple: one important task, one body/health action and one personal rule. Your history suggests three meaningful wins work better than a huge list.",
  losing:"Your evening rules are the most fragile part of your routine. Move one of them earlier in the day rather than adding pressure.",
};
export function CoachPage(){const [q,setQ]=useState("");const [a,setA]=useState("");function ask(){const x=q.toLowerCase();setA(x.includes("better")?answers.better:x.includes("tomorrow")?answers.tomorrow:x.includes("losing")?answers.losing:"Across your recent activity, protecting your first meaningful block is your strongest consistency pattern.");}return <section className="page"><p className="eyebrow">YOUR PERSONAL AI COACH</p><h1>Let's figure out today together. 🧠</h1><p className="muted">Ask about your consistency, patterns, goals or what to do next.</p><article className="insight"><strong>✨ I noticed something</strong><p>Your strongest days usually begin with one meaningful action before scrolling.</p></article><div className="chips">{["Why was this week better?","What should I focus on tomorrow?","Where am I losing consistency?"].map(x=><button key={x} onClick={()=>setQ(x)}>{x}</button>)}</div><textarea value={q} onChange={e=>setQ(e.target.value)} placeholder="Ask naturally..." /><button className="primary" onClick={ask}>Ask ConsistencyAI →</button>{a&&<article className="insight"><strong>🧠 ConsistencyAI says</strong><p>{a}</p></article>}</section>}
